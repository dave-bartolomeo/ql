import cpp

/**
 * Function: example:
 * ```cpp
 * int foo();
 * ```
 */

Function anyFunction() {
  result = result
}

from Function f
select f.getBlock(), "Message: $@", f, f.getLocation().getFile().getAbsolutePath()
